# 발표 예상 질문 & 답변 총정리

---

## Core 시스템

**Q. 미니게임 클리어 연결을 왜 인터페이스로 구현했나요?**

"미니게임마다 클리어 조건이 전부 다릅니다. 테트리스는 라인 클리어, 1942는 보스 처치, 포켓몬은 배틀 승리처럼요. `MiniGameManager`가 각 게임의 내부를 알 필요 없이 `OnGameClear()`와 `OnGameFail()` 두 메서드만 호출하면 허브로 복귀할 수 있도록 인터페이스로 계약을 맺었습니다. 덕분에 팀원 각자가 독립적으로 작업할 수 있었습니다."

---

**Q. 그냥 추상 클래스로 해도 되지 않나요?**

"추상 클래스는 단일 상속만 가능합니다. 각 컨트롤러가 이미 `MonoBehaviour`를 상속받고 있어서 추상 클래스를 추가로 상속받을 수 없었습니다. 인터페이스는 다중 구현이 가능하기 때문에 함께 쓸 수 있었습니다."

---

**Q. 인터페이스 메서드가 두 개뿐인데 너무 단순한 거 아닌가요?**

"의도적입니다. `MiniGameManager`가 각 게임에 대해 알아야 하는 건 클리어됐는가, 실패했는가 딱 두 가지입니다. 내부 로직은 각 게임이 책임지고 허브는 결과만 받으면 됩니다. 역할 분리를 명확히 하기 위해 최소한으로 유지했습니다."

---

**Q. 씬 전환은 어떻게 구현했나요?**

"Additive Scene Loading 방식입니다. 허브 씬을 언로드하지 않고 미니게임 씬을 위에 올리는 구조라, 미니게임이 끝나면 씬을 언로드하고 허브 오브젝트를 다시 활성화합니다. 전환 시 BGM과 SFX도 강제 종료해서 소리가 겹치는 문제를 방지했습니다."

---

## 파워퍼프걸 (1942 슈팅)

**Q. 웨이브 시스템을 어떻게 구현했나요?**

"코루틴으로 Wave1(30초) → Wave2(30초) → 보스 순서로 흐름을 관리했습니다. 각 웨이브마다 적 속도, 사격 패턴, 이동 패턴을 확률로 조합해서 매 판마다 다른 느낌이 나도록 했습니다."

---

**Q. 보스가 죽었을 때 클리어 처리를 어떻게 연결했나요?**

"`BossController`에 `OnBossDefeated` 이벤트를 선언하고 `WaveManager`가 구독해서 `OnGameClear()`를 호출하는 구조입니다. 보스와 게임 흐름 관리가 서로를 직접 참조하지 않아 결합도를 낮췄습니다."

---

**Q. 컷씬 중에 플레이어 조작을 어떻게 막았나요?**

"보스 사망 루틴 시작 시점에 `PlayerController.enabled = false`로 컴포넌트를 비활성화하고 Rigidbody velocity를 즉시 0으로 초기화했습니다. 처음엔 게임 클리어 시점에만 막았다가 컷씬 대화 구간에서도 움직이는 버그가 발견돼서 보스 사망 루틴 시작 직후로 앞당겼습니다."

---

**Q. 적 오브젝트를 풀링 없이 매번 생성/파괴하면 성능 문제가 없나요?**

"한 화면에 동시에 존재하는 적 수가 많지 않고 웨이브 간격으로 순차 소환되기 때문에 GC 부하가 체감될 만큼 크지 않았습니다. 실제로 플레이 중 프레임 드랍 없이 동작하는 걸 확인했습니다. 오브젝트 풀링을 적용하면 더 최적화할 수 있는 부분이라는 건 인지하고 있습니다."

---

## 포켓몬 (디지몬 × 포켓몬)

**Q. 트레이너 시야 감지를 어떻게 구현했나요?**

"원작처럼 가로/세로 직선 시야만 감지하도록 구현했습니다. 플레이어와의 거리 벡터를 계산해서 수평 또는 수직 오차가 허용 범위 이내일 때만 감지하는 방식입니다. 콜라이더 없이 수식만으로 처리해서 씬이 가볍습니다."

---

**Q. 배틀 씬 전환 없이 배틀을 어떻게 구현했나요?**

"씬 전환 없이 같은 씬 안에서 배틀 UI 패널을 활성화하는 방식입니다. 맵 오브젝트는 그대로 두고 배틀 패널이 위에 올라오는 구조라 로딩이 없고, 패배 후 블랙아웃 → 리스폰도 자연스럽게 같은 씬 안에서 처리됩니다."

---

**Q. 패배 시 연출을 어떻게 처리했나요?**

"'눈앞이 깜깜해졌다' 메시지 → BGM 정지 + 블랙아웃 + 회복 SFX 동시 재생 → SFX 길이 + 1초 대기 → 리스폰 + 필드 BGM 재생 순서입니다. SFX 길이를 `AudioClip.length`로 동적으로 읽어서 클립이 바뀌어도 타이밍이 자동으로 맞춰집니다."

---

## 스폰지밥 (역전재판)

**Q. 재판 대사 데이터를 어떻게 관리했나요?**

"대사 데이터를 JSON 파일로 분리해서 `Resources` 폴더에서 로드했습니다. 시나리오가 수정돼도 코드를 건드릴 필요 없이 JSON만 수정하면 됩니다."

---

**Q. 증언 낭독과 심문을 어떻게 구분했나요?**

"`Testifying`과 `CrossExamination` 두 상태로 분리했습니다. 낭독 중에는 타이핑 연출로 순서대로 읽고, 심문 중에는 플레이어가 앞뒤로 자유롭게 이동하며 Q키 추궁과 증거 제출이 가능합니다. 페이드 코루틴으로 두 상태 전환 시 연출을 넣었습니다."

---

**Q. 2라운드 재증언은 어떻게 처리했나요?**

"`StartRetestimony()`에서 증언 배열만 `retestimonyLines`로 교체하면 같은 로직이 그대로 재사용됩니다. 1라운드와 2라운드가 구조적으로 완전히 동일해서 코드 중복이 없습니다."

---

## 테트리스 (케로로)

**Q. 테트리스 회전이 벽에서 어떻게 동작하나요?**

"실제 테트리스 원작 규격인 SRS(Super Rotation System)를 구현했습니다. 회전 시 막히면 미리 정의된 킥 테이블의 오프셋을 순서대로 시도해서 가능한 위치를 찾습니다. I블록은 별도 킥 테이블을 사용합니다."

---

**Q. 바닥에서 무한정 버틸 수 있지 않나요?**

"Lock Delay와 최대 조작 횟수 제한을 함께 적용했습니다. 바닥에 닿은 후 0.5초 안에 고정되고, 바닥에서의 조작이 15회를 초과하면 강제로 고정됩니다. 실제 테트리스와 동일한 방식입니다."

---

**Q. DAS/ARR이 뭔가요?**

"조작감을 결정하는 값입니다. DAS는 방향키를 누른 후 연속 이동이 시작되기까지의 딜레이, ARR은 연속 이동 간격입니다. 인스펙터에서 조절 가능하게 해서 원하는 조작감으로 튜닝할 수 있습니다."

---

## 슈가슈가룬

**Q. 주변 여학생이 난이도에 영향을 준다고 했는데 어떻게 구현했나요?**

"`Physics2D.OverlapCircleAll`로 타겟 주변 반경 내 여학생 수를 매 프레임 계산해서, 여학생이 많을수록 홀드 게이지 차오르는 속도는 느려지고 클래시 게이지 깎이는 속도는 빨라집니다. 자연스럽게 주변 상황이 곧 난이도가 됩니다."

---

**Q. 하트 스프라이트가 중복으로 나오지 않게 어떻게 했나요?**

"Fisher-Yates 셔플로 하트 스프라이트 풀을 섞은 뒤 앞에서부터 순서대로 꺼내는 방식입니다. 풀이 비면 다시 채워서 셔플합니다. 같은 하트가 연속으로 나오지 않습니다."
